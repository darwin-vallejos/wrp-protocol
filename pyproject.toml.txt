[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "wrp-protocol"
version = "0.1.0"
description = "Witness Receipt Protocol CLI"
requires-python = ">=3.9"

[project.scripts]
wrp = "wrp_protocol.cli:cli"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
